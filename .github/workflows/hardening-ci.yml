name: Hardening CI

on:
  push:
    branches:
      - audit/hardening-20260913
  pull_request:
    branches:
      - main

permissions:
  contents: write

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: package.json
      - name: Apply one-time hardening patch
        run: python3 scripts/apply-hardening-patch.py
      - run: npm install --no-audit --no-fund
      - run: npm run lint
      - run: npm run build
      - name: Persist tested source and lockfile
        if: github.event_name == 'push'
        run: |
          if git diff --quiet -- server.ts package-lock.json; then
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add server.ts package-lock.json
          git commit -m "security: apply verified server hardening"
          git push origin HEAD:audit/hardening-20260913
