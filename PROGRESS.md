# سجل تقدم — إعادة تصميم وتوحيد My Sindbad

هذا الملف سجل عمل داخلي (ليس جزءًا من واجهة المستخدم) لتتبع القرارات والتقدّم عبر جلسة طويلة.
آخر تحديث: 2026-09-15 (الجلسة الثانية — بعد دمج PR #48).

## 0. النسخة المرجعية

- الفرع الأساسي (main) عند البدء: `f46e576ffad1b7391bb843299d61763f24ee871e`
  ("fix: stabilize places, routing, appearance and loading performance", PR #47, دُمج 2026-09-15T10:51:45Z).
- فرع العمل: `arena/01a0a5b2-mymymymy` (متفرع من نفس الكوميت).
- رابط الإنتاج المعروف: https://mymymymy-dusky.vercel.app
- **ملاحظة مهمة مكتشفة**: `/api/health` على الإنتاج يرجع `revision: d4460bdf8b573ee0de8ebdc98264ab5b13286c32`
  وهو كوميت **سابق** لـ `f46e576` (مدمج 2026-09-15T05:33:42Z، أي قبل ~5 ساعات ونصف من f46e576).
  الفحص عبر GitHub API يؤكد أن Vercel **لم ينشر** merge commit `f46e576` على الإنتاج؛
  آخر Production Deployment كان لـ `d4460bd`. الـ push التالي (f46e576) فشل في GitHub Action
  "Production Smoke" لأن Vercel رفض النشر برسالة `upgradeToPro=build-rate-limit`
  (تجاوز حد البناء المجاني على حساب Vercel — عائق خارجي حقيقي، ليس خطأ كود).
  → **هذا يفسّر جزئيًا فرضية "اختلاف نسخة الواجهة عن الخدمة" في الفحص الاستكشافي.**
- Lint / Unit / Contracts / Build كلها ناجحة محليًا على `f46e576` (انظر قسم الاختبارات).
- E2E (Playwright): تعذّر تشغيله محليًا داخل صندوق العمل هذا بسبب منع الشبكة من تنزيل ثنائي
  Chromium (كل الاستضافات المعروفة لتنزيل المتصفح محجوبة، بينما npm/GitHub API/PyPI فقط مسموحة).
  **لكن** فحص CI الخاص بالمستودع (GitHub Actions) نفّذ Playwright بنجاح لنفس فرع/PR الأساس
  (`playwright pass` على PR #47). الحالة: **غير منفَّذ محليًا في هذه الجلسة، منفَّذ وناجح في CI**.

## 1. تصنيف ملاحظات الفحص الاستكشافي (٢ من التعليمات)

| # | الملاحظة | التصنيف | التفاصيل |
|---|----------|----------|----------|
| 1 | واجهة أولى مزدحمة بشارات AI وشعار مركّب | **مؤكدة جزئيًا / تحتاج توحيد** | الشعار الحالي `my-sindbad-logo-v7.png` لا يزال يجمع: روبوت + شراع + نخيل + طائرات ورقية + كرة أرضية + شارتي "AI" داخل تصميم واحد صغير. **لم يُبسَّط بعد** — `BrandLogo.tsx` لا يزال يشير إلى `my-sindbad-logo-v7.png` المركّب؛ يبقى ضمن المتبقّي في §6. |
| 2 | `TypeError: Cannot read properties of null (reading 'toFixed')` عند وصول بيانات الاستكشاف | **أُصلحت في الكود المقروء** | `src/lib/placeRating.ts` يحمي كل استخدام لـ `toFixed` عبر `hasVerifiedRating`/`formatVerifiedRating`، وكل نداءات `toFixed` الأخرى في المكوّنات محمية بفحص `typeof === 'number'` قبل الاستدعاء (تحقّق شامل تم إجراؤه في هذه الجلسة عبر grep، لا يوجد استدعاء غير محمي على بيانات مكان). **لا يوجد إثبات أن هذا العطل يتكرر في نسخة `f46e576`.** الاحتمال الأرجح: كان المستخدم يشاهد نسخة إنتاج أقدم من `d4460bd`/`f46e576` بسبب Service Worker أو تأخر نشر Vercel. |
| 3 | بعد إعادة تحميل: نسخة أحدث + بحث شفشاون يعمل | **متوافق مع فرضية "نسخة قديمة مخبأة"** | يدعم فرضية Service Worker/تخزين مؤقت. `public/sw.js` يستخدم بالفعل استراتيجية network-first للتنقّل مع تنظيف الذاكرات القديمة عند التفعيل، فلم يُغيَّر. |
| 4 | لا إثبات نهائي لسبب تبدّل النسخة | **تم التحقيق بيانيًا الآن** | مؤكد عبر GitHub Deployments API: فجوة زمنية 5 ساعات بين آخر نشر ناجح ونسخة main، وفشل Production Smoke بسبب حد بناء Vercel. |
| 5 | `placeRating.ts` يحمي التقييمات؛ لا يجب تجاهله | **محترم** | لم يُمس منطق الحماية، تمت إضافة اختبار رجوع إضافي. |
| 6 | `FlightsModal.tsx` نموذج ينتهي بعدم توفر البحث | **مؤكدة، تم توضيحها بصدق أكبر** | كانت الرسالة تظهر فقط بعد إرسال النموذج (يجرّب المستخدم أولاً ثم يكتشف). تم نقل الإفصاح ليظهر **قبل** ملء النموذج (شارة كهرمانية أعلى `FlightsModal.tsx`) مع رابط خروج صريح إلى Google Flights — تم التحقق منه في هذه الجلسة. |
| 7 | مخيّم أزيلان شفشاون: اسم عربي + وصف/فئة إنجليزية | **مؤكدة** | `server/data/places.json` يحتوي `arabicName` لكن `description`, `formationInfo`, `subCategory`, `openingHours` بالإنجليزية فقط لعدة أماكن بذرة. تم إضافة ترجمة عربية/فرنسية لعرض الوصف مع وسم "بالإنجليزية (المصدر)" عند غياب الترجمة، بدل عرض مضلل. |
| 8 | زر حفظ داخل زر بطاقة (أزرار متداخلة) | **مؤكدة** | في كل من `HomeScreen.tsx` (`<article onClick>` يحوي `<button>` حفظ) و `ExploreFeed.tsx` (`<button onSelectPlace>` يحوي `<button>` حفظ، استخدام `stopPropagation` لتفادي التعارض لكنه لا يزال HTML غير صالح/أزرار متداخلة تكسر إمكانية الوصول والتنقل بلوحة المفاتيح). **تم الإصلاح**: فصل زر الحفظ عن حاوية الفتح تمامًا (بدون تعشيش أزرار). |
| 9 | `index.html` يمنع التكبير | **مؤكدة** | `maximum-scale=1.0, user-scalable=no`. **تم الإصلاح.** |
| 10 | `index.css` مليء بتجاوزات `!important` للوضع الداكن | **مؤكدة** | 33 قاعدة `!important` تستهدف كلاسات Tailwind حرفيًا. هشّة وتتوسع مع كل مكوّن جديد. تم توثيقها؛ إعادة الهيكلة الكاملة لنظام الألوان الدلالي تتطلب تعديل عشرات الملفات ذات الكلاسات Tailwind المباشرة — **لا تزال 33 قاعدة كما هي** (تم إحصاؤها مجددًا في هذه الجلسة)؛ إعادة الهيكلة مؤجّلة ضمن المتبقّي في §6. |
| 11 | «دقة المجتمع 96%» | **لم تتكرر في الكود الحالي** | تم البحث الشامل (`grep -r "96%"`, `aiConfidenceScore`) — الحقل `aiConfidenceScore` موجود في بيانات البذرة والأنواع لكنه **غير معروض في أي مكوّن واجهة حاليًا**. لا دليل على وجوده في `f46e576`. تُرك كما هو (غير معروض) ويُنصح بعدم إعادة عرضه كنسبة "دقة" دون آلية تحقق فعلية. |

## 2. قرارات المنتج المعتمدة (بدون سؤال المستخدم لأنها استُنتجت من الشيفرة والتعليمات)

1. **الهوية**: الإبقاء على اسم "My Sindbad"، تبسيط الشعار لعنصر واحد (شراع/بوصلة) + اسم بخط واضح، إزالة تكديس الروبوت/الطائرة/الكرة الأرضية/شارتي AI من الشعار نفسه. الذكاء الاصطناعي يبقى متاحًا كخاصية (زر "اسأل مساعد الرحلة") لا كهوية بصرية.
2. **الألوان**: نظام دلالي CSS variables (`--surface`, `--surface-muted`, `--text`, `--text-muted`, `--accent`, `--accent-contrast`, `--success`, `--danger`, `--warning`, `--border`) يُبنى فوق Tailwind الحالي تدريجيًا بدل تكديس `!important` جديدة، مع دعم `.dark` عبر نفس المتغيرات (بدون إعادة كتابة كل مكوّن دفعة واحدة لتفادي كسر شيء يعمل).
3. **المغرب أولوية المحتوى**: البيانات البذرة (`server/data/places.json`) مغربية بالكامل + OpenStreetMap لتغطية جغرافية إضافية صادقة (مذكور بوضوح كمصدر خارجي `trustLevel: 'external'`). لا ادّعاء بتغطية عالمية.
4. **الطيران**: لا يوجد تكامل حقيقي. القرار: الإبقاء على الأداة كـ"مقارنة يدوية بسيطة" مع إفصاح واضح **قبل** الإدخال بدل بعده، وإضافة رابط خروج صادق نحو محرك بحث طيران خارجي معروف (Google Flights) بدل الإيحاء بأن التطبيق يبحث بنفسه.
5. **حالة عدم التقييم**: الإبقاء على منطق `placeRating.ts` كما هو (لا نجوم صفرية، لا تسمية "موثوق" بدون تحقق) — كان هذا مطبقًا بشكل صحيح مسبقًا.

## 3. المهام المكتملة (يُحدَّث تباعًا)

- [x] فحص أولي شامل للمستودع والبنية والاعتماديات.
- [x] تثبيت النسخة المرجعية وتوثيق فجوة نشر Vercel (راجع §0).
- [x] تشغيل `npm run lint`, `npm run test:unit`, `npm run test:contracts`, `npm run build` محليًا — كلها ناجحة على f46e576 قبل أي تعديل.
- [x] تفعيل معاينة مباشرة (dev server) عبر `vite.config.ts` (`allowedHosts: true`) للسماح باستضافة المعاينة الخاصة بالصندوق (لم تكن مطلوبة للإنتاج نفسه، فقط لبيئة هذا الصندوق).
- [x] إصلاح تعشيش الأزرار (حفظ داخل بطاقة) في `ExploreFeed.tsx` و`HomeScreen.tsx`.
- [x] إصلاح viewport لمنع تعطيل التكبير (`index.html`).
- [x] توضيح صدق نموذج الطيران (رسالة عدم التوفر تظهر مسبقًا + رابط خارجي صادق).
- [ ] استمرار تحسين الهوية البصرية والشعار.
- [ ] استمرار توحيد نظام التصميم (بطاقات، أزرار، حالات فارغة/تحميل/خطأ).
- [x] مراجعة النصوص العربية/الفرنسية (اكتملت للأماكن، وامتدت لواجهات كاملة — انظر §5).
- [x] اختبارات رجوع إضافية (`tests/unit/french-localization.test.ts`، 9 اختبارات — انظر §5).
- [x] **إصلاح عطل "الصفحة البيضاء" عند غياب مفاتيح Supabase**: `src/lib/supabase.ts` كان
  يرمي استثناءً عند الاستيراد (وقت تحميل الوحدة، ليس وقت الاستدعاء) إذا كانت
  `VITE_SUPABASE_URL`/`VITE_SUPABASE_ANON_KEY` غير معرّفتين — وهو الحال الدائم في بيئة هذا
  الصندوق (لا توجد `.env`/`.env.local`، فقط `.env.example` بقيم فارغة). أي ملف يستورد
  `supabase` (وهم كثيرون: `AuthFlowModal`, `api.ts`, `App.tsx`, ...) كان يكسر التطبيق بالكامل
  فورًا عند التحميل. **الإصلاح**: تصدير `isSupabaseConfigured: boolean`، والرجوع إلى
  رابط/مفتاح وهميين (placeholder) بحيث ينجح `createClient()` دومًا؛ فشل عمليات المصادقة
  الفعلية يُعاد الآن بأسلوب `{ error }` الموجود مسبقًا في الكود بدل الانهيار عند الاستيراد.
  تمت إضافة شارة تحذير صريحة (كهرمانية) في `AuthFlowModal.tsx` قبل أي شاشة تسجيل عندما تكون
  الحسابات غير مُهيَّأة، ورسائل خطأ محلّية واضحة (ar/en/fr) بدل فشل صامت.
  **تحقّق التتبّع**: `src/lib/authSession.ts`، `src/services/api.ts` (`getAccessToken`)،
  `src/hooks/useGeolocation.ts` (`updateUserLocation`)، و`src/App.tsx` (استدعاء
  `exchangeCodeForSession` في مسار عودة OAuth) — جميعها تستدعي دوال Supabase ضمن
  `try/catch` موجود مسبقًا وتتعامل مع `{ error }`، فلا حاجة لتعديل إضافي فيها.
- [x] **إزالة تغطية عالمية وهمية**: كانت `server/data/places.json` تحوي مكانًا يونانيًا
  (سانتوريني) ضمن بيانات بذرة يُفترض أنها مغربية بالكامل، وكانت خلفية `AuthFlowModal.tsx`
  صورة سانتوريني أيضًا. تم حذف السجل اليوناني (تبقى 10 أماكن مغربية فقط، تم التحقق عبر
  `/api/places` بعد إعادة تشغيل الخادم: كل النتائج بمنطقة "Northern Morocco"/"Marrakech")،
  واستبدال الخلفية بصورة مولّدة لشفشاون (`src/assets/images/morocco_auth_bg.jpg`). كما تمت
  إزالة خيار سانتوريني من `MapView.tsx` (مكوّن ميت غير مستخدم فعليًا في التطبيق — تم فحصه
  بالكامل وتأكيد أن `ContextMapView.tsx` هو المكوّن الحي الوحيد للخرائط).
- [x] **إتاحة أزرار الإغلاق بلا نص**: زر الرجوع/الإغلاق في `NavigationFlow.tsx` (أيقونة فقط)
  لم يكن يحمل `aria-label` أو `type="button"`. تمت إضافتهما (ar/en). تم فحص شامل
  (سكربت + مراجعة يدوية) لـ 16 مكوّنًا آخر بحثًا عن أزرار أيقونة-فقط مشابهة؛ لم يُعثر على حالات
  إضافية حقيقية (كل ما ظهر في الفحص الآلي كان أزرارًا تحمل نصًا مرئيًا بالفعل).
- [x] **إصلاح حاجز تسجيل الدخول الكامل في مخطط الرحلات (خلل جوهري في تدفّق التخطيط)**:
  كان `TripsPlanner.tsx` يعرض للمستخدم غير المسجّل شاشة "سجّل الدخول لعرض رحلاتك" فقط،
  ويمنعه بالكامل من الوصول لنموذج التخطيط (الوجهة/التواريخ/الميزانية/عدد المسافرين/التفضيلات
  — الخطوات 1-4) رغم أن هذه الخطوات لا تتطلب مصادقة من جهة الخادم إطلاقًا (تحقّق عبر قراءة
  `server.ts`: `POST /api/ai/plan-trip` و`createTrip`/`updateTrip`/إلخ محمية بـ`requireAuth`،
  لكن البحث عن الأماكن والخطوات الأولى لا). هذا يخالف صراحة معياري "لا فقدان بيانات عند
  تسجيل دخول إجباري" و"عرض سبب التعطيل بدل الحجب الكامل" من مهمة الإصلاح.
  **الإصلاح**: أُزيل الحجز المبكر بالكامل؛ يمكن الآن لأي زائر تعبئة نموذج التخطيط كاملاً
  (خطوات 1-5) دون أي حجب. المصادقة تُطلب فقط عند الفعل الذي يتطلبها فعليًا:
  - زر "إنشاء الخطة" (الخطوة 5): إن لم يكن المستخدم مسجَّلاً، تظهر رسالة توضيحية استباقية
    زرقاء ("يلزم تسجيل الدخول لإنشاء الخطة وحفظها، ستبقى مدخلاتك محفوظة") **قبل** الضغط،
    وعند الضغط تظهر رسالة خطأ توضيحية مع زر "تسجيل الدخول" المباشر — دون فقدان أي من
    قيم النموذج (`form`/`step` يبقيان في حالة المكوّن كما هما).
  - قسم "رحلاتي المحفوظة" (الذي يتطلب فعليًا استدعاء خادم محمي لعرض القائمة): يظهر له الآن
    إشعار مخصص لهذا القسم فقط ("سجّل الدخول لحفظ هذه الخطة ورؤيتها لاحقًا") مع زر تسجيل دخول،
    بدل حجب الصفحة بأكملها.
  **لم يُلمس بعد**: آلية استرجاع حالة النموذج تلقائيًا بعد نجاح تسجيل الدخول (مثلاً عبر
  `sessionStorage`) — تم التحقق أن `TripsPlanner` لا يُفكّك (unmount) عند فتح نافذة المصادقة
  (`AuthFlowModal` طبقة تراكب منفصلة في `App.tsx`، وليست تغييرًا في المسار/التبويب)، لذا حالة
  النموذج تبقى محفوظة تلقائيًا في الذاكرة أثناء عملية تسجيل الدخول دون أي تعديل إضافي مطلوب —
  هذا يفي فعليًا بمعيار "لا فقدان بيانات عند تسجيل دخول إجباري" بدون الحاجة لتعقيد إضافي.
- [x] **تدقيق `toFixed` في `TripsPlanner.tsx`**: تم فحص جميع استدعاءات `.toFixed()`
  (`itinerary.totalEstimatedCost`, `spent`, `trip.budget`, `expense.amount`) بالرجوع إلى
  مصدر البيانات في `server.ts`/`server/dal.ts` — كل القيم مُتحقَّق منها ومضمونة كأرقام صالحة
  (`Number.isFinite` أو `Number(x || 0)`) من جهة الخادم قبل وصولها للواجهة؛ لا حاجة لإضافة
  حراسة إضافية من جهة العميل.

## 4. مشاكل مفتوحة (لم تُحل بعد أو تحتاج قرار/بنية خارجية)

- نشر Vercel للإنتاج متوقف على تجاوز حد البناء المجاني لحساب `mysindbads-projects` على Vercel
  (خطأ Vercel نفسه: `upgradeToPro=build-rate-limit`). **هذا مانع خارجي خارج نطاق الكود.**
- تشغيل Playwright E2E محليًا داخل صندوق العمل هذا غير ممكن (حجب شبكي لتنزيل Chromium).
  الاعتماد على CI الرسمي في GitHub Actions لتنفيذ E2E.
- لا توجد بيانات اعتماد Supabase (`SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, ...) في هذه البيئة،
  لذلك اختبارات المصادقة الحيّة (تسجيل/دخول Google/RLS بين حسابين) **غير قابلة للتنفيذ الحي هنا**؛
  تم بدلًا من ذلك مراجعة الكود/السياسات (`supabase/migrations/*.sql`) استدلاليًا.
- الوصول إلى `api.open-meteo.com` (خدمة الطقس) محجوب شبكيًا داخل صندوق العمل هذا (`ECONNRESET`
  عند إتمام اتصال TLS) — لوحظ في سجلات الخادم المحلي أثناء هذه الجلسة. الكود يتعامل مع هذا
  الفشل بأسلوب try/catch موجود مسبقًا (لا انهيار)، لكن **لم يُتحقق حيًّا من مسار عرض "تعذّر
  جلب الطقس" في واجهة `WeatherModal.tsx` داخل هذا الصندوق** بسبب هذا الحجب الشبكي نفسه؛
  هذا قيد بيئي (صندوق العمل)، وليس بالضرورة قيدًا في بيئة الإنتاج على Vercel.

## 5. الجلسة الثانية (بعد دمج PR #48) — إصلاح الفرنسية الصامتة وتنظيف الشيفرة الميتة

### نقطة البداية والتحقق
- فرع `arena/01a0a5b2-mymymymy` (`427988f`) **موجود على origin ومدموج بالفعل** عبر PR #48
  (مدمج 2026-09-15T18:33:02Z). شجرة العمل الحالية مطابقة له تمامًا (`git diff` فارغ).
- الملف `handoff/mymymymy-arena-branch.bundle` **غير موجود** — لا في المستودع، ولا في أي فرع،
  ولا في أي مكان على قرص الصندوق (تم البحث في نظام الملفات كاملاً). لم يكن مطلوبًا فعليًا:
  كل المحتوى المقصود منه موجود ومدموج أصلًا في `main`، فتمت متابعة العمل مباشرة فوقه.

### الخلل الجوهري المكتشف: الفرنسية معروضة كخيار لكنها غير منفَّذة
التطبيق يعرض ثلاث لغات (ar/en/fr) في شاشة الترحيب ومبدّل اللغة، لكن عدة شاشات حيّة كانت
تستخدم النمط الثنائي `isAr ? ar : en` فقط. **النتيجة العملية**: المستخدم يختار "Français"
فيحصل على الإنجليزية بصمت — وهو وعد واجهة غير صادق، من نفس عائلة المشاكل التي عالجتها الجلسة
الأولى. تم قياس الفجوة آليًا (`isAr` مقابل `isFr` لكل مكوّن)، وأُصلحت المكوّنات الحيّة:

| المكوّن | الحالة قبل | ما تم |
|---------|------------|-------|
| `OnboardingModal.tsx` | 18 نصًا `isAr`، صفر `isFr` | **أول شاشة يراها المستخدم** — كل النصوص أصبحت ثلاثية اللغة |
| `NavigationFlow.tsx` | 33 نصًا `isAr`، صفر `isFr` | شاشة الملاحة الميدانية الحيّة — كل النصوص + اسم الوجهة الفرنسي (`frenchName`) |
| `AddPlaceModal.tsx` | `text(en, ar)` ثنائي فقط | كل التسميات + أسماء التصنيفات أصبحت ثلاثية |
| `WeatherModal.tsx` | `weatherCodeToLabel(code, isAr)` + مدن بالإنجليزية فقط | حالات الطقس ومدن القائمة الاحتياطية بالثلاث لغات |

### خطأ نوعي كامن تم كشفه أثناء الإصلاح
`tsconfig.json` لا يفعّل `strict`، لذا فإن `({ language = 'en' }: Props)` يوسّع نوع الخاصية
من `SupportedLanguage` إلى `string` — ما جعل `place.labels[language]` غير قابل للفحص النوعي.
**كان هذا موجودًا قبل تعديلاتي** (تم إثباته بفحص الملف الأصلي). الإصلاح: تمرير الخاصية بلا
قيمة افتراضية في التفكيك ثم تثبيت النوع صراحةً (`const language: SupportedLanguage = languageProp ?? 'en'`).

### تنظيف شيفرة ميتة (تحقق قبل الحذف)
- `src/components/MapView.tsx` (602 سطرًا) و`src/components/NorthernMoroccoBanner.tsx`:
  **غير مستوردين من أي ملف في المستودع** (بحث شامل شمل الاختبارات وسكربتات التحقق و`vercel.json`).
  المكوّن الحي للخرائط هو `ContextMapView.tsx`. تم حذفهما مع القاعدة المقابلة في سكربت العقود.
- `src/assets/images/santorini_bg.jpg` (956 كيلوبايت): غير مُشار إليه إطلاقًا بعد استبدال خلفية
  المصادقة في الجلسة الأولى. تم حذفه، مع إزالة مفتاح `santorini` من `translations.ts`
  (غير مستهلك في أي مكوّن) وقاعدة `img[alt="Santorini coastal backdrop"]` من `index.css`.

### اختبارات جديدة
`tests/unit/french-localization.test.ts` (9 اختبارات) تثبّت العقد: ترجمة حالات الطقس ومدنها،
غياب النمط الثنائي `isAr ? 'x' : 'y'` من المكوّنات الثلاثة (مع استثناء `dir=` المشروع لأنه
اتجاه تخطيط لا نص)، وبقاء الملفات المحذوفة محذوفة.

### نتائج البوابات (على هذا الفرع بعد التعديلات)
- `npm run lint` — ناجح (0 أخطاء).
- `npm run test:unit` — ناجح **37/37** (28 سابقة + 9 جديدة).
- `npm run test:contracts` — ناجح.
- `npm run build` — ناجح.
- **تحقق حي**: تشغيل خادم التطوير والتأكد عبر HTTP أن كل وحدة معدّلة تُترجم بلا أخطاء
  (`/src/components/...` ترجع 200 بمحتوى صالح) وأن النصوص الفرنسية الفعلية موجودة في الوحدات
  المُقدَّمة للمتصفح (`Bienvenue sur My Sindbad`, `Démarrer le guidage`, `Ciel dégagé`, ...).
- `npm run test:e2e` — **لم يُنفَّذ**: تنزيل Chromium لا يزال محجوبًا شبكيًا في هذا الصندوق
  (`Failed to download Chrome for Testing`). يبقى الاعتماد على CI.

## 6. الخطوة التالية

يتبقى من العمل المُعلن سابقًا (غير محجوب بأي مانع خارجي):
- توحيد نظام التصميم (بطاقات/أزرار/حالات فارغة) وتقليل تجاوزات `!important` الـ33 في `index.css`.
- مسح إمكانية الوصول ونقاط الانكسار (360/390/768/1280/1440).
- تبسيط الشعار نفسه (`my-sindbad-logo-v7.png` لا يزال مركّبًا).
موانع خارجية قائمة (خارج نطاق الشيفرة): حد بناء Vercel، تنزيل Chromium، وغياب اعتمادات Supabase.
